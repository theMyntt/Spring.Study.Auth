spring:
  application:
    name: demo
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=auth_db;trustServerCertificate=true
    username: sa
    password: "*123456HAS*"
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
